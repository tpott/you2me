<% /* help me */ %>
<ul id="albums">
	<% for (var album in info) { /* TODO info? */ %>
	<li>
		<h2><%= album.name %></h2><br />
		<ol>
			<% for (var track in album) { %>
			<li><a href="<%= track.link %>"><%= track.name %></a></li>
			<% } %>
		</ol>
	</li>
	<% } %>
</ul>
